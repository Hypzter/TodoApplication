name: Reminder

on: workflow_dispatch

jobs:
  birthday-reminder:
  
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
    
      - name: Run reminder script
        run: |
          bash /OneDrive/Systemutveckling .net Vis Stud/Clean Code & Devops/ToDoWithDanielsStructure/birthday_reminder.sh
